# 安全说明

## 报告问题

如果你发现了安全问题，请开一个 issue，或者在必要时直接联系仓库所有者。密码管理器的漏洞影响很大，请不要公开利用细节后再通知。

## 设计取舍

### 为什么用 PBKDF2 而不是 Argon2id

Argon2id 抗 GPU 更好，这是事实。但没有官方 Android 实现，引入第三方原生库会给一个体量很小的应用带来数 MB 体积、多 ABI 兼容性和供应链风险。本项目的定位是个人可审计的轻量工具，因此选了平台内置的 PBKDF2-HMAC-SHA256，并把迭代次数按设备校准。

如果你更看重抗暴力破解能力，换 Argon2id 是合理的改进方向。

### 为什么头部不加密

盐和迭代次数不是秘密，写在明文头部可以：

1. 在不知道密码时读出 KDF 参数
2. 区分「密码错」与「文件损坏」
3. 让未来调整参数成为可能，不会把老数据锁死

### 为什么保留旧密钥

`LegacyCrypto` 里保留了 v2.x 的硬编码密钥，这是为了让老用户能把自己被旧版本加密的数据导出来。这条路径**只做解密，不做加密**。任何新数据都不会再用它。

如果你从没用过 v2.x，可以删掉 `LegacyCrypto.java` 和 `LegacyImporter.java`，应用照常工作。

### 已知限制

- 密钥在解锁期间存在于内存中。JVM 无法保证可靠地擦除，`wipe()` 只是尽力而为。
- 没有防护内存转储或调试器附加。
- 剪贴板中的密码可能被其他应用读取（Android 10 以下尤其明显）。
